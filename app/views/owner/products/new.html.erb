<h3>商品　新規登録画面</h3><br>
<%= form_for(@product, url: new_owner_product_path) do |f| %>
    <%= f.attachment_field :image %>

      <div class="form-group">
        <label>商品名</label>
          <%= f.text_field :name %>
      </div>

      <div class="form-group">
        <label>商品説明</label>
          <%= f.text_field :information %>
      </div>

      <div class="form-group">
        <label>ジャンル</label>
          <%= f.collection_select :genres_id, Genre.all, :id, :name %>
      </div>

      <div class="form-group">
        <label>税抜価格</label>
          <%= f.text_field :price %>円
      </div>

      <div class="form-group">
        <label>販売ステータス</label>
      </div>

      <div class="form-group">
        <%= f.submit '新規登録' %>
      </div>
<% end %>